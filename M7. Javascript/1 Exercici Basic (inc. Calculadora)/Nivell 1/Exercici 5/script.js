var nota_examen=Math.random()*10;

if (nota_examen>=5) {
    alert("Has aprobado el examen con un "+nota_examen.toFixed(1));
} else {
    alert("Has suspendido el examen con un "+nota_examen.toFixed(1));
}
